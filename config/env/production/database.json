{
    "defaultConnection": "default",
    "connection": {
      "client": "postgres",
    "connection": {
      "connectionString": "${process.envprocess.env.DATABASE_URL}",
      "ssl": {
        "rejectUnauthorized": false
      }
    }
  }
}
